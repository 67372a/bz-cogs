---
trigger: always_on
---

# Git Rules

## Instructions for Agent
The agent is STRICTLY PROHIBITED from running deciding to run any git commands. Do not attempt to commit, push, pull, branch, stash, or perform any other operation using `git`.

If the user directly requests git related actions, then you may run them.
